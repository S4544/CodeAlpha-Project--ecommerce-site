# Django E-Commerce Site - CodeAlpha  Project 

A fully functional basic e-commerce platform built with Django.

## ✨ Features
- **Product Management**: Dynamic product listing and detailed view pages.
- **User Authentication**: Secure registration and login functionality.
- **Shopping Cart**: Session-based cart allowing users to add/remove items.
- **Restricted Checkout**: Checkout process is locked to logged-in users only.
- **Admin Dashboard**: Custom link for staff to manage products and view orders.

## 🚀 Technical Stack
- **Backend**: Django (Python)
- **Frontend**: HTML5, CSS3 (using Django Template Language)
- **Database**: SQLite (default Django DB)

## 📦 Installation
1. Clone the repo: `git clone https://github.com/S4544/my-ecommerce-site.git`
2. Install dependencies: `pip install -r requirements.txt`
3. Run migrations: `python manage.py migrate`
4. Start server: `python manage.py runserver`
